import React from "react";

const navbar = ({brand}) => {
    return(
        <nav>
            <div>
                <a href="#!">{brand}</a>
            </div>
        </nav>
    )
}

export default navbar;